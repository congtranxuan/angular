import { Providerregisterform } from './providerregisterform';

describe('Providerregisterform', () => {
  it('should create an instance', () => {
    expect(new Providerregisterform()).toBeTruthy();
  });
});
